import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'gaandeweg-client',
  webDir: '../../dist/apps/gaandeweg-client',
  bundledWebRuntime: false,
};

export default config;
